fill missings

1. class - if missing fill with 0
2. newspaper - if missing fill with 2
3. born - if missing fill with 1
4. method - if missing reject row

preli norm

1. replace 2 with 0 for (1,2) class variable

extra

1.delete rows (if want more children != 2)
